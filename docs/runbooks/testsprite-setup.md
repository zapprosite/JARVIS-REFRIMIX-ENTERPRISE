## TestSprite MCP Integration

**Status**: Blocked (OAuth2 authentication pending)

**Workaround**: Use manual test generation via:
```bash
npm run test:generate -- --service=orchestrator
```

**Roadmap**: Sprint 4 (after MCP auth spec stabilizes)
